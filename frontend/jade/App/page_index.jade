extends ./main.jade

mixin article(title)
    article
        h3= title
        if block
            block
        else
            p= 'Nada a declarar, por enquanto'

block content
    session#main_content.inner
        +article('A história de Pedro')
            include:markdown articles/historia-pedro.md